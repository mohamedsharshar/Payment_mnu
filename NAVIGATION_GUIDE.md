# 🔗 روابط الوصول إلى النظام

## 📱 **بوابة الطالب (Student Portal)**

### الروابط المباشرة:

1. **الصفحة الرئيسية:**
   ```
   http://127.0.0.1:8000/student/dashboard
   ```

2. **بيانات الكلية:**
   ```
   http://127.0.0.1:8000/student/faculty
   ```

3. **الإيصالات (مع بحث وتصدير Excel):**
   ```
   http://127.0.0.1:8000/student/receipts
   ```

---

## 🔐 **تسجيل الدخول كطالب:**

1. افتح: `http://127.0.0.1:8000/login`
2. اختر من القائمة: **طالب (Student)**
3. أدخل:
   - **البريد:** student@student.com
   - **كلمة المرور:** password
4. اضغط تسجيل الدخول
5. سيتم توجيهك تلقائياً إلى: `/student/dashboard`

---

## 👨‍💼 **لوحة الأدمن (Admin Panel)**

### الروابط المباشرة:

1. **Dashboard:**
   ```
   http://127.0.0.1:8000/admin/dashboard
   ```

2. **العملاء:**
   ```
   http://127.0.0.1:8000/admin/customers
   ```

3. **الإيصالات:**
   ```
   http://127.0.0.1:8000/admin/bills
   ```

4. **التقارير:**
   ```
   http://127.0.0.1:8000/admin/reports
   ```

---

## 🔐 **تسجيل الدخول كأدمن:**

1. افتح: `http://127.0.0.1:8000/login`
2. اختر من القائمة: **مدير النظام (Admin)**
3. أدخل:
   - **البريد:** admin@admin.com
   - **كلمة المرور:** password
4. اضغط تسجيل الدخول
5. سيتم توجيهك تلقائياً إلى: `/admin/dashboard`

---

## 🛡️ **الحماية (Middleware):**

### ✅ **ماذا يحدث الآن بعد التحديث:**

1. **إذا كنت مسجل كـ Student وحاولت الدخول لصفحة Admin:**
   - ✅ سيتم توجيهك إلى `/student/dashboard`
   - ✅ رسالة: "أنت مسجل كطالب. لا يمكنك الوصول إلى لوحة تحكم الأدمن"

2. **إذا كنت مسجل كـ Admin وحاولت الدخول لصفحة Student:**
   - ✅ سيتم توجيهك إلى `/admin/dashboard`
   - ✅ رسالة: "أنت مسجل كأدمن. لا يمكنك الوصول إلى بوابة الطالب"

3. **إذا لم تكن مسجل دخول:**
   - ✅ سيتم توجيهك إلى `/login`

---

## 📝 **أمثلة عملية:**

### مثال 1: أنت طالب وتريد رؤية إيصالاتك
```
1. سجل دخول كـ Student
2. اكتب في المتصفح: http://127.0.0.1:8000/student/receipts
3. أو اضغط على "الإيصالات" في القائمة الجانبية
```

### مثال 2: أنت طالب وحاولت الدخول للأدمن عن طريق الخطأ
```
1. لو كتبت: http://127.0.0.1:8000/admin/dashboard
2. سيحولك تلقائياً إلى: http://127.0.0.1:8000/student/dashboard
3. مع رسالة تحذير
```

### مثال 3: أنت أدمن وحاولت الدخول لبوابة الطالب
```
1. لو كتبت: http://127.0.0.1:8000/student/dashboard
2. سيحولك تلقائياً إلى: http://127.0.0.1:8000/admin/dashboard
3. مع رسالة تحذير
```

---

## 🔍 **كل Routes المتاحة:**

### Student Routes:
- `/student/dashboard` - الصفحة الرئيسية
- `/student/faculty` - بيانات الكلية
- `/student/receipts` - قائمة الإيصالات
- `/student/receipts?status=1` - الإيصالات المعلقة فقط
- `/student/receipts?status=2` - الإيصالات المدفوعة فقط
- `/student/receipts?date_from=2025-01-01&date_to=2025-12-31` - بحث بالتاريخ
- `/student/receipts/export` - تصدير Excel

### Admin Routes:
- `/admin/dashboard` - الصفحة الرئيسية
- `/admin/customers` - إدارة العملاء
- `/admin/bills` - إدارة الإيصالات
- `/admin/services` - إدارة الخدمات
- `/admin/payments` - المدفوعات
- `/admin/reports` - التقارير

### Public Routes:
- `/login` - تسجيل الدخول
- `/logout` - تسجيل الخروج

---

## 💡 **نصيحة:**

**للطالب:** احفظ الرابط المباشر
```
http://127.0.0.1:8000/student/dashboard
```

**للأدمن:** احفظ الرابط المباشر
```
http://127.0.0.1:8000/admin/dashboard
```

---

## ✅ **الخلاصة:**

بعد التحديث:
- ❌ **لن تظهر صفحة 403 بعد الآن**
- ✅ **سيتم التوجيه التلقائي للصفحة الصحيحة**
- ✅ **رسائل واضحة ومفهومة**
- ✅ **حماية كاملة للصفحات**
